apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: postgresql-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/deployments/default/nextcloud/postgresql-helm-values
  type: Opaque