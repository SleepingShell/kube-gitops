grafana:
  ingress:
    annotations:
      cert-manager.io/cluster-issuer: ca-issuer
    hosts:
      - monitor.$DOMAIN
    tls:
    - hosts:
      - monitor.$DOMAIN
      secretName: grafana-tls
  adminPassword: $GRAFANA_PASSWORD